# skillcorner-hackathon

[uv](https://docs.astral.sh/uv/) manages Python and dependencies for this repo.

```bash
uv sync              # create .venv and install deps from pyproject.toml
uv run python main.py
```

Install uv: see [Installing uv](https://docs.astral.sh/uv/getting-started/installation/).
